# E2B API Key for running performance tests
E2B_API_KEY=your_api_key_here

# Optional: MCP command to test (can also be passed as argument)
MCP_COMMAND=npx -y @modelcontextprotocol/server-sequential-thinking
